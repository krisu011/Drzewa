/*package org.example;

public class KNNeighbor {
    public static double distanceCalcuclator(Map mapa, int x_parent, int y_parent) {
        for (int y = 0; y < map.length; y++) {
            for (int x = 0; x < map[y].length; x++) {
                if (map[y][x] != null) {
                    return  Math.sqrt(Math.pow(x_parent - x, 2) + Math.pow(y_parent - y, 2));
                }
            }
        }
    }
}*/
